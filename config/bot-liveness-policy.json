{
  "_schema": {
    "version": 2,
    "purpose": "Declare each bot's deployment mode + intended state so watchdog liveness checks match reality and the desired-state reconciler can restore intent.",
    "modes": {
      "systemd": "Bot runs under a systemd unit. Liveness = `systemctl is-active <unit>`. Recovery = `sudo systemctl restart <unit>`.",
      "tmux-or-systemd": "Bot may run in a tmux session OR a systemd unit. Liveness is satisfied if EITHER path is alive. Recovery defaults to the tmux launcher (the intended deployment) so two competing processes are not started."
    },
    "desired_state": {
      "values": ["up", "down"],
      "default": "up",
      "purpose": "Operator-declared intent. The watchdog reconciler restarts `up`+down bots and suppresses the down alert for `down`+down bots — replacing the mask-unit/kill-healer hack used to hold a bot off."
    },
    "default_mode": "systemd",
    "default_mode_note": "If a bot is missing from this file, watchdog assumes mode=systemd with unit `ourliberty-<agent>-bot.service` — preserves current behavior for beacon/forge/mirror should the file lose an entry.",
    "doctrine": "docs/runbooks/watchdog-bot-policy.md (rule + enforcement pairing per doctrine-of-doctrine, PR #163)."
  },
  "beacon": {
    "mode": "systemd",
    "desired_state": "up",
    "systemd_unit": "ourliberty-beacon-bot.service"
  },
  "forge": {
    "mode": "systemd",
    "desired_state": "up",
    "systemd_unit": "ourliberty-forge-bot.service"
  },
  "mirror": {
    "mode": "systemd",
    "desired_state": "up",
    "systemd_unit": "ourliberty-mirror-bot.service"
  },
  "pulse": {
    "mode": "systemd",
    "desired_state": "up",
    "systemd_unit": "ourliberty-pulse-bot.service"
  }
}
