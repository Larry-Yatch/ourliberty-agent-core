{
    "_doc": "Trust policy for Beacon's approval gate. First-match-wins; default_action applies when no rule matches. Edit this file to add carve-outs as Larry builds confidence in specific dispatch shapes. Reloaded on every evaluate() call so changes take effect without restart. See scripts/trust_policy.py for the evaluator and schema.",
    "_rule_fields": {
        "source": "agent/system that initiated the task. \"*\" or omitted matches any.",
        "target": "agent receiving the dispatch. \"*\" or omitted matches any.",
        "task_type": "label on the task envelope. \"*\" or omitted matches any.",
        "repos": "glob list against task.target_repo. [] matches any.",
        "file_patterns": "glob list against task.changed_files. [] matches any.",
        "action": "one of auto_approve | force_ask | reject."
    },
    "_example_rules_disabled": [
        {
            "source": "pulse",
            "target": "forge",
            "task_type": "doc-only",
            "repos": [
                "ourliberty-agent-core"
            ],
            "file_patterns": [
                "docs/**",
                "*.md"
            ],
            "action": "auto_approve",
            "_note": "Pulse-flagged doc-only changes in agent-core auto-approve. Uncomment to enable."
        }
    ],
    "version": 1,
    "default_action": "force_ask",
    "rules": [
        {
            "source": "pulse-auto-dispatch",
            "target": "*",
            "action": "auto_approve",
            "_note": "AUTONOMY (2026-06-22, Larry-approved). Pulse-auto-dispatch envelopes auto-fire without Larry's pre-build click. SAFE because the envelope only asks Beacon to DRAFT A SPEC (target_agent=beacon, no build), and Pulse only auto-dispatches candidates that are effort=small AND $-quantified, always targeting ourliberty-agent-core (pulse_check_i._is_auto_dispatch_eligible / _build_dispatch_envelope). The downstream build (Beacon->Forge->agent-core) is still governed by the rules below + Mirror REVIEW_PASS before any merge + classify_careful exclusion + EMERGENCY_HALT/pause. This removes ONLY the 'draft a spec?' click. REVERT = set action back to force_ask (re-read on every evaluate(), no restart). Was force_ask (closed-loop v1 conservatism, 2026-05-24)."
        },
        {
            "source": "beacon",
            "target": "forge",
            "repos": ["ourliberty-agent-core"],
            "file_patterns": [
                "config/**",
                "systemd/**",
                "deploy/**",
                ".github/**",
                "migrations/**",
                "**/migrations/**",
                "supabase/migrations/**",
                "scripts/active_tier.py",
                "scripts/rotate_active_tier.py",
                "scripts/kill_switch.py",
                ".env*",
                "**/.env*",
                "credentials/**"
            ],
            "action": "force_ask",
            "_note": "Sensitive-path carve-out (2026-06-21). A Beacon\u2192Forge agent-core dispatch that DECLARES it touches config/systemd/deploy/CI/migrations/account-tier/kill-switch/secrets still asks Larry. CAVEAT (trust_policy._matches_file_patterns): file_patterns only match when the task carries changed_files \u2014 true for revisions/re-dispatches and any envelope that enumerates files, but NOT a fresh build request (the files don't exist yet). For fresh dispatches the real guards are (a) the board-drain's classify_careful gate (keeps deploy/migrate/credential cards out of the autonomous supply) and (b) Mirror's review + ESCALATE/halt before any merge. This rule is the secondary net + intent marker. MUST stay ordered BEFORE the broad auto_approve rule (first-match-wins)."
        },
        {
            "source": "beacon",
            "target": "forge",
            "repos": ["ourliberty-agent-core"],
            "action": "auto_approve",
            "_note": "AUTONOMY (2026-06-21, Larry-approved 'low-risk code, agent-core'). Beacon\u2192Forge dispatches targeting ourliberty-agent-core auto-fire without Larry's pre-build click. Downstream gates REMAIN fully in force \u2014 Mirror REVIEW_PASS before any auto-merge, file-overlap merge serializer, GitHub-truth merge confirmation, EMERGENCY_HALT, /pause. This removes ONLY the pre-dispatch human click; it never auto-merges unreviewed code. Sensitive paths are carved to force_ask by the preceding rule (see its CAVEAT). REVERT = delete this rule (re-read on every evaluate(), no restart). Other repos + pulse-auto-dispatch still force_ask."
        }
    ]
}
